import React, { FC } from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-paper';

const Home: FC = () => {
    return(
        <View>
            <Text>
                toto
            </Text>
        </View>
    );
};

export default Home;
